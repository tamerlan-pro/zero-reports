{
  "name": "zero-reports",
  "private": true,
  "workspaces": [
    "packages/*",
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm run build -w packages/zero-theme && npm run build -w packages/contracts && npm run build -w client && npm run build -w server",
    "lint": "npm run lint -w client && npm run lint -w server",
    "test": "npm run test -w client && npm run test -w server",
    "format": "prettier --write \"**/*.{ts,tsx,json}\" --ignore-path .gitignore"
  },
  "devDependencies": {
    "prettier": "^3.4.2"
  }
}
